version: 2.1

orbs:
  python: circleci/python@1.2.1

jobs:
  build-and-test:
    executor: python/default
    steps:
      - checkout
      - run:
          command: python JEWebSocket/Testpy/Websocket_Server_Test.py
          name: Test

workflows:
  main:
    jobs:
      - build-and-test